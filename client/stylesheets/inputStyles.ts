import { StyleSheet } from "react-native";

export const inputStyles = StyleSheet.create({
  input: {
    height: 100,
    width: "auto",
    marginBottom: 0,
    color: "white",
    borderWidth: 1,
    borderColor: "black",
    padding: 40,
  },
});
