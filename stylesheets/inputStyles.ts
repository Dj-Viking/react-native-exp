import { StyleSheet } from "react-native"
export const inputStyles = StyleSheet.create({
  input: {
    height: 40,
    margin: 12,
    color: "white",
    borderWidth: 1,
    borderColor: "black",
    padding: 10,
  }
});